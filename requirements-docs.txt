# Documentation site dependencies. Install into a uv-managed venv:
#   uv venv && uv pip install -r requirements-docs.txt
# Then: uv run mkdocs serve
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-exclude==1.0.2
